<div class="module hero-landing">
    {% if module.image %}
    {% set image = TimberImage(module.image) %}
    <img alt="{{image.alt}}" class="mw-100" src="{{image.src|resize(3088, 1448)}}">
    {% endif %}
</div>